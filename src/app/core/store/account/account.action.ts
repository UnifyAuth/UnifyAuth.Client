import { createAction, props } from '@ngrx/store';

export const startEmailChange = createAction('[Account] Start Email Change');
export const emailChangeSuccess = createAction(
  '[Account] Email Change Success'
);
